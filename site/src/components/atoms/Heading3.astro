---
import type { ColorType } from "@/types/index";

interface Props {
	baseColor: ColorType;
	id: string;
}

const { baseColor, id } = Astro.props;
---

<h3
    class={`flex break-words text-lg font-bold md:text-xl ${baseColor.text}`}
    id={id}><slot /></h3
>
